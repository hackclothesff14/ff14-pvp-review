# DB バックアップの考え方

Stella Note のデータ（`reviews` テーブル）を守るためのバックアップ方針です。

---

## 1. Supabase 標準のバックアップ

Supabase は **全プランで毎日** データベースのバックアップを自動取得しています。

| プラン | バックアップの保持期間 |
|--------|------------------------|
| **Free** | 毎日取得されるが、保持期間・ダッシュボードからの復元は Pro 以上を推奨。将来は無料では日次バックアップがなくなる可能性あり |
| **Pro** | 過去 **7 日分** の日次バックアップ + **PITR（Point-in-Time Recovery）** オプションで数分単位で復元可能 |
| **Team / Enterprise** | 保持期間がさらに長い |

**確認方法**

- [Supabase ダッシュボード](https://supabase.com/dashboard) → 対象プロジェクト → **Database** → **Backups**

無料プランの場合は「いつかバックアップが使えなくなる」可能性を前提に、**自分でエクスポートを取る**仕組みを併用することをおすすめします。

---

## 2. 自分で取るバックアップ（推奨）

### A. アプリの「エクスポート API」を使う（手軽）

このプロジェクトには **全件 JSON エクスポート** 用の API を用意しています。

**手順**

1. ブラウザまたは `curl` で次の URL にアクセスする（本番の場合は本番 URL に置き換え）  
   `https://あなたのサイトのURL/api/backup`
2. 表示された JSON をそのままコピーし、ローカルに `.json` ファイルとして保存する  
   - 例: `stella-note-backup-2026-03-18.json`

**オプション: アクセス制限**

- バックアップ用 URL を他人に知られたくない場合は、環境変数 `BACKUP_SECRET` を設定する  
  - 例: `BACKUP_SECRET=あなたの秘密の文字列`
- その場合、次の形式でアクセスする必要があります。  
  `https://あなたのサイトのURL/api/backup?secret=あなたの秘密の文字列`
- `BACKUP_SECRET` を設定していない場合は、`/api/backup` だけでエクスポートできます。

**定期的に取る**

- 週 1 回など、カレンダーでリマインダーを入れて手動でアクセスし、JSON を保存する
- または GitHub Actions などで定期実行し、JSON を artifact や別ストレージに保存する（要設定）

---

### B. Supabase ダッシュボードからエクスポート

1. Supabase ダッシュボード → **Table Editor** → **reviews**
2. 一覧表示された状態で、必要に応じて **Export** や CSV 出力ができる場合があります（UI はバージョンにより異なります）

---

### C. Supabase CLI でダンプ（上級者向け）

ローカルに [Supabase CLI](https://supabase.com/docs/guides/cli) を入れ、プロジェクトとリンクしたうえで:

```bash
supabase db dump -f backup.sql
```

で SQL ダンプを取得できます。DB のスキーマ＋データをまとめてバックアップしたい場合に便利です。

---

## 3. 復元の考え方

| バックアップの種類 | 復元方法 |
|-------------------|----------|
| **Supabase の日次バックアップ** | ダッシュボードの **Database > Backups** から、復元したい日時を選んで復元（Pro 以上で利用しやすい） |
| **PITR（Pro のオプション）** | 数分単位で「あの時刻の DB」に戻せる。誤削除・誤更新の復旧に強い |
| **JSON エクスポート（/api/backup）** | アプリ側で「インポート」機能を用意するか、Supabase の Table Editor や SQL で 1 件ずつ/一括で `reviews` に再投入する必要あり（現状は手動） |

---

## 4. おすすめの運用

1. **Supabase のバックアップ**  
   - 有料プランなら「Database > Backups」を定期的に確認し、保持期間を把握しておく。
2. **自分用の保険**  
   - 週 1 回などで `/api/backup` にアクセスし、JSON をローカルやクラウドストレージに保存する。
3. **重要度が高くなったら**  
   - Supabase の Pro プラン + PITR を検討し、障害や誤操作時にすぐ戻せるようにする。

---

## 5. 環境変数（バックアップ API 用）

| 変数名 | 必須 | 説明 |
|--------|------|------|
| `BACKUP_SECRET` | 任意 | 設定すると、`/api/backup?secret=ここに同じ値` のときだけエクスポート可能。未設定なら誰でも `/api/backup` で取得可能。 |

本番でバックアップ URL を隠したい場合は、Vercel の Environment Variables に `BACKUP_SECRET` を追加してください。
