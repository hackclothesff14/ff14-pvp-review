# データ復旧手順（振り返り・反省内容が消えた場合）

## 1. まず確認すること

**Supabase ダッシュボード**で、該当レコードの `content` カラムを確認してください。

1. [Supabase](https://supabase.com/dashboard) にログイン
2. プロジェクトを開く → **Table Editor** → **reviews** テーブル
3. **3/12** の日付（`review_date` が `2026-03-12` の行）のレコードを探す
4. **content** 列を見る

- **文字が入っている** → データは DB に残っています。表示不具合の可能性があるので、ブラウザのキャッシュ削除や再読み込みで確認してください。
- **空になっている** → いったん空で上書きされた状態です。以下の「2. 復旧方法」を検討してください。

---

## 2. 復旧方法（content が空の場合）

### A. Supabase のバックアップ・PITR を使う（推奨）

- **Pro プラン**では **Point-in-Time Recovery (PITR)** が使えます。  
  過去の時点の DB に戻すことで、上書き前の `content` を取り戻せる可能性があります。
- 手順は [Supabase の PITR ドキュメント](https://supabase.com/docs/guides/platform/backups#point-in-time-recovery-pitr) を参照してください。

### B. 手動で再入力する

- バックアップが使えない場合は、該当の「振り返り」を再度入力して保存する必要があります。

---

## 3. 今後の防止策（今回の対応内容）

**振り返り（content）が空で上書きされないように**、保存処理を修正しました。

- 編集画面で「更新」を押したときに、画面上の振り返りがまだ反映されていない場合でも、**既存の振り返りが空で上書きされない**ようになっています。
- この対応は今回の修正から有効です。今後の誤上書きは防げます。

---

## 4. 復旧できたあと

復旧後は、該当レコードの編集画面を開き、振り返りが正しく表示・保存されるか確認してください。問題があれば、開発者に連絡するか、Issue で報告してください。
