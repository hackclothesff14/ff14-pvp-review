# Supabase でデータをどこで見るか

このアプリの**画面ごとのデータ**は、すべて **同じ1つのテーブル** に入っています。

---

## テーブルは「reviews」だけ

| 場所 | 見方 |
|------|------|
| **Supabase ダッシュボード** | **Table Editor** → **reviews** を開く |

ここに、TOP・編集・分析の**すべてのデータ**が入っています。別のテーブルは使いません。

---

## 1行 = 1件の「反省会記録」

**reviews** の **1行（1レコード）** が、アプリ上の「1件の反省会記録」です。

| カラム名 | 意味 | どの画面で使うか |
|----------|------|------------------|
| **id** | その記録のID | 編集ページのURL（/reviews/〇〇/edit）に使う |
| **review_date** | 試合日付 | TOP一覧の「日付」、分析の日付絞り |
| **opponent** | 対戦相手 | TOP一覧の「対戦相手」、編集画面の入力欄 |
| **content** | 振り返り（旧：反省内容） | 編集画面の「振り返り」欄の本文 |
| **video_url** | 動画URL（JSON文字列） | 編集画面の動画リンク |
| **matches** | 試合ごとの結果（JSON文字列） | 編集画面の試合ブロック、分析ページの勝率集計 |
| **members** / **jobs** | メンバー・ジョブ（互換用） | 現在はほぼ未使用 |

- **TOP画面** … **reviews** の全行を一覧表示しているだけです。
- **〇〇の編集ページ** … **reviews** の「id がその〇〇の行」の1行を表示・編集しています。
- **分析ページ** … 同じ **reviews** の **matches** を集計してMAP別勝率を出しています。

なので、「各ページの内容のデータ」はすべて **reviews** の「行」と「カラム」で見られます。

---

## Supabase で中身を確認するとき

1. **Table Editor** → **reviews** を開く。
2. 一覧で **1行クリック** すると、その1件の全カラムが表示されます。
3. **content** … 振り返りの本文（編集画面の「振り返り」と同じ）。
4. **matches** … 試合ごとのデータが JSON で入っています（長い文字列なので、セルを広げるかクリックして全体を見る）。

「TOPのDBしか見れていない」場合は、**reviews の横スクロール**で右側の **content** や **matches** まで見るか、**行をクリックして詳細**を開くと、各ページで表示している内容が同じテーブルの別カラムとして確認できます。
