# 開発の進め方（本番を壊さず・データを守る）

チームに公開したあと、機能追加・改修を安全に進めるための方針です。

---

## 1. 基本の流れ：ローカル → Git push → 本番反映

**この形で問題ありません。**

1. **ローカル**でコードを編集する（`npm run dev` で動作確認）
2. 問題なければ **Git に commit → push** する
3. **Vercel が main ブランチの push を検知して自動で本番デプロイ**する

いまの構成（GitHub + Vercel 連携）なら、この流れだけで「本番＝常に main の最新」が保たれます。

---

## 2. ブランチを分けるか

**必須ではありませんが、やると安全です。**

| やり方 | 向いている場面 |
|--------|----------------|
| **main だけで開発** | 自分だけの開発、小さい変更だけするとき |
| **機能ごとにブランチ** | 大きい機能追加・「試したい」変更をするとき |

**おすすめの運用（シンプル版）**

- **main** ＝ 本番にそのまま出ているコード。**触るとすぐ本番に反映される**ので、直すとき以外は直接 commit しない。
- **機能を追加するとき**は、`feature/〇〇` のようなブランチを切ってそこで作業し、動くことを確認してから main に **merge** する。

```bash
# 例：新機能用ブランチを作って作業
git checkout -b feature/新機能の名前
# 編集して commit
git add .
git commit -m "〇〇を追加"
git push -u origin feature/新機能の名前
```

その後、GitHub で **Pull Request (PR)** を作成し、main に merge すれば、Vercel が自動で本番デプロイします。

---

## 3. Staging（本番の前に試す環境）は必要？

**必須ではありませんが、あると「本番が壊れる」リスクを減らせます。**

**Vercel のプレビュー機能 = 無料の Staging**

- **main 以外のブランチ**を push すると、Vercel が **そのブランチ用のプレビュー URL** を自動で発行します。
- 例：`feature/新機能` を push → `https://ff14-pvp-review-xxx-**feature-新機能**.vercel.app` のような URL ができる。
- この URL で「本番と同じ動き」を確認してから、main に merge すると安全です。

**まとめ**

- 小さな修正だけ → main で直して push でも可。
- **「本番が壊れたら困る」変更** → ブランチを切る → プレビュー URL で確認 → main に merge、という流れにすると安心です。

---

## 4. データが見えなくなる・復元できなくなる事態を防ぐ

### 4-1. コード側でやっていること（すでに実装済み）

- **`parseMatchResults`**  
  既存データの形式が変わっても、`normalizeMatch` でできるだけ読めるようにしています。パースに失敗しても **DB の文字列は上書きしない**（`preserveMatchesRef`）ようにしてあります。
- **フォーマットを変えるとき**  
  `constants.ts` のコメントのとおり、**旧形式も読める**ようにしておくことが大事です。

### 4-2. 改修時に心がけること

1. **保存形式（matches / video_url の JSON）を変えるとき**  
   - 新しい形式で **保存** しても、**読み込み時は旧形式も受け付ける**ようにする。  
   - 例：新しいキーを追加するなら、`normalizeMatch` で「そのキーがなくてもデフォルトで動く」ようにする。

2. **「場当たり的な変更」を減らす**  
   - 動くかどうか分からない大きな変更は **main に直接 push しない**。  
   - ブランチで試す → プレビューで確認 → 問題なければ main に merge、にすると「本番が壊れて復元できない」事態を避けやすいです。

3. **Supabase のデータ**  
   - 本番の **reviews** テーブルは Supabase 上にのみあります。  
   - Supabase のダッシュボードから **バックアップ・エクスポート** の有無を確認し、重要な運用が始まったら定期的なバックアップを検討すると安心です。

### 4-3. 壊れたときの復元

| 対象 | 復元の考え方 |
|------|----------------|
| **コード** | Git の **過去の commit に戻す**（`git revert` や `git reset`）。Vercel は「前のデプロイにロールバック」も可能。 |
| **DB のデータ** | コードのパース不具合なら、**コードを直して再デプロイ**すれば、DB に残っているデータは再度読める（上書きを防いでいるため）。Supabase のバックアップがあれば、テーブル単位の復元も検討可。 |

---

## 5. 日々の開発チェックリスト（簡易版）

- [ ] 大きな変更は **ブランチを切って** から編集
- [ ] 変更後は **ローカルで `npm run build` が通る** ことを確認
- [ ] 保存形式（matches など）を変えたら **旧データも読める** ようにする
- [ ] 本番に出す前に、可能なら **Vercel のプレビュー URL** で一度確認
- [ ] merge や push のたびに **「何を変えたか」を commit メッセージに書く**

この運用にしておけば、「ローカルで変更 → Git に push」を基本にしつつ、本番を壊さず、データが見えなくなる・復元できなくなるリスクをかなり減らせます。
